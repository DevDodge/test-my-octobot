// This file is kept minimal - OAuth SDK has been removed.
// Authentication is now handled via simple email/password login.
// JWT session management is in context.ts and oauth.ts (login route).
export {};
